# 2026-09-21 初始审查

结论：发布受限响应脚本；暂不采用上游最新完整模块。未发现本次发布入口明显恶意行为，不等同于证明绝对安全。

上游提交：65075cdb388fc5e3094afd7e7314c67b243f3525。响应脚本 Git blob 校验为 becad8eaa6094c189ea8db6d644de68ac2d66f61。源码中大部分为 protobuf 类型与编解码库，应用逻辑位于文件后段。

审查覆盖：入口和 URL 分派、广告处理、请求与响应访问、持久化、网络接口、动态代码执行、通知与日志。字符串检查未发现 eval、new Function 或 require 调用；唯一 HTTP URL 字符串为 protobuf 库错误信息中的文档链接。通用网络适配器有 $httpClient / $task.fetch 定义，但当前应用入口没有调用它们。静态检查和合成测试不能证明任意输入下无漏洞。

## 暂不采用的变化

上游 `youtube.request.js` 指定 `https://init-stream.maasea.workers.dev/`，将 clientKey 编码到 ck 参数，并将原始播放请求 URL 编码到 target 参数，然后返回 URL 改写结果。这将把播放相关数据交给第三方服务。原请求头/正文是否随转发传递取决于客户端行为，本次没有实机验证，不能断言一定泄露登录 Cookie。Worker 服务实现未在所读脚本中提供，不能据此完成服务端审查，也不能仅凭此断言维护者有恶意。

该转发不在用户原先提供的模块配置内。因此当前保留原有响应过滤和 `oad` 拦截，排除 request.js 及 config/log_event 密钥收集入口。该选择可能使新播放机制下的去广告效果弱于最新完整上游。

## 修改

- 保留 Ni/Ir/Di 广告处理，以及 get_watch 中相同的广告处理。
- Br 只调用 Ni；不再调用播放能力修改或字幕处理。
- 移除 guide/get_setting/config/log_event 分派入口；模块只匹配六类广告相关响应。
- 不读取/写入 YouTubeConfig；缓存改为独立的 YouTubeReviewedAdvertiseInfo，避免与旧模块混用。
- 路径分派改用最后一个路径段精确匹配，避免查询参数干扰。
- 解析异常不输出响应内容或错误文本，直接保留原响应。
- 原打包文件中的未使用类型/函数仍保留，减少大规模重写；不得把它们的存在误称为正在执行。

## 验证

15 个合成测试通过：player 广告字段删除、get_watch 嵌套处理、Shorts 显式广告标志删除、空列表接口、排除接口、畸形 protobuf 回退、查询参数分派，以及无播放解锁/无密钥访问/无网络调用断言。来源 Git blob 已校验。此结果不使用真实账户数据，不代表 iOS 测试。

待实测：私库请求头是否作用于当前小火箭版本的资源下载、首次启动鉴权顺序、一天刷新设置、各 YouTube/YouTube Music 页面及播放广告效果。没有凭空记录测试成功。


## 1.0.1：公开分发（2026-09-21）

用户手动将仓库改为公开，已用匿名 GitHub API 核实。删除令牌参数、Authorization 请求头规则及 raw.githubusercontent.com 解密主机。新增标准模块链接，旧 Private 文件名保留为相同无鉴权内容的兼容入口。响应脚本内容未变，因此不改变已审查的广告逻辑与限制。手机播放及自动刷新仍待实测。
